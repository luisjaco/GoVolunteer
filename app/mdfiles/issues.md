1. Project's code lines were all red:
Solution: npm install
          npm install expo

2. Expo Router installer error:
Solution:
React is on version 19.1.0
React-dom requires version 19.2.0

run "npx expo install react react-dom react-native" in terminal will install updated versions 
run "npx expo install expo-router" to complete setup

3. How to use Expo icons

npx expo install @expo/vector-icons