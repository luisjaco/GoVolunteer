// layout.jsx = a component and template that wraps pages
// Ex: -> a footer can be put into a layout file so that it appears on every page automatically
// Ex: -> NavBar that appears on every page should be here as well

 import { Stack } from "expo-router"; 
   
    export default function Layout() {
  return <Stack />;
}